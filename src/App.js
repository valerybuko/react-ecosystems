import React from 'react';
import { hot } from 'react-hot-loader';
import './App.css'

const App = () => (
    <h1 className="App">React Application Container</h1>
);

export default hot(module)(App);